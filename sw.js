const CACHE_NAME = "pocketmind-shell-v1200";
const NEWS_CACHE = "pocketmind-news-v1200";
const APP_SHELL = ["./","./index.html","./app.js","./cpu.js","./news-v09.js","./web-search.js","./webllm-worker.js","./manifest.webmanifest","./icons/icon-192.png","./icons/icon-512.png","./icons/maskable-512.png"];
self.addEventListener("install",event=>{event.waitUntil(caches.open(CACHE_NAME).then(cache=>cache.addAll(APP_SHELL)));self.skipWaiting();});
self.addEventListener("activate",event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>![CACHE_NAME,NEWS_CACHE].includes(k)).map(k=>caches.delete(k)))));self.clients.claim();});
self.addEventListener("fetch",event=>{const req=event.request;if(req.method!=="GET")return;const url=new URL(req.url);if(url.origin!==self.location.origin)return;const isPage=req.mode==="navigate"||url.pathname.endsWith("/")||url.pathname.endsWith("/index.html"),isNews=url.pathname.endsWith("/news.json");if(isNews){event.respondWith((async()=>{try{const fresh=await fetch(req,{cache:"no-store"});if(fresh&&fresh.ok){const cache=await caches.open(NEWS_CACHE);await cache.put("./news.json",fresh.clone());}return fresh;}catch(_){const cache=await caches.open(NEWS_CACHE);return(await cache.match("./news.json"))||new Response(JSON.stringify({error:"news unavailable"}),{status:503,headers:{"content-type":"application/json"}});}})());return;}if(isPage){event.respondWith((async()=>{try{const fresh=await fetch(req,{cache:"no-store"});if(fresh&&fresh.ok){const cache=await caches.open(CACHE_NAME);await cache.put(req,fresh.clone());}return fresh;}catch(_){return(await caches.match(req))||(await caches.match("./index.html"));}})());return;}event.respondWith(caches.match(req).then(cached=>cached||fetch(req).then(async resp=>{if(resp&&resp.ok){const cache=await caches.open(CACHE_NAME);await cache.put(req,resp.clone());}return resp;})));});
